{
  "name": "imagnx",
  "version": "0.1.4",
  "description": "Multi-model image generation CLI",
  "license": "MIT",
  "author": "Henrique Ferrer",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/henriquerferrer/imagnx.git"
  },
  "type": "module",
  "bin": {
    "imagnx": "dist/cli.js"
  },
  "files": [
    "dist",
    "LICENSE",
    "CHANGELOG.md",
    "README.md"
  ],
  "scripts": {
    "start": "tsx src/cli.ts",
    "test": "vitest run",
    "test:integration": "vitest run tests/integration",
    "smoke": "tsx scripts/smoke.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && chmod +x dist/cli.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "citty": "^0.1.6",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "~5.9.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
